# User

#### Getting a profile from the database

Keys are the column names in the database, values are the values in the database.

Possible keys are:
- accountId

###### Example:

```typescript
const profile = await momentum.getProfile("coolaccountid"); //Getting a profile by its accountId.
```